<%= form_for team do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%=f.fields_for :coach do |coach| %>

    <%= coach.label :name %>
    <%= coach.text_field :name %>

    <%= coach.label :email %>
    <%= coach.text_field :email %>

    <%= coach.label :password %>
    <%= coach.password :password %>

    <%= player.label :password_confirmation %>
    <%= player.password :password_confirmation %>

  <% end %>

  <%= f.fields_for :players do |player| %>

   <%= player.label :name %>
   <%= player.text_field :name %>

   <%= player.label :email %>
   <%= player.text_field :email %>

   <%= player.label :password %>
   <%= player.password :password %>

   <%= player.label :password_confirmation %>
   <%= player.password :password_confirmation %>

  <% end %>

  <%= f.submit %>
<% end %>
